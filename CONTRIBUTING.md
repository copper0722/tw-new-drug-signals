# 貢獻指南

這個 repo 的內容會被引用到臨床、法規與政策的討論裡，而且掛在具名的執業醫師底下。
所以這裡最貴的失敗不是「寫得不完整」，是「自信地寫錯」。下面三條規則都是為了
擋住那一種。

---

## 1. 任何數字都不得裸奔

每一個百分比、每一個計數，都必須同時帶著：

- **量測日期**
- **母體大小 n**
- **重跑方式**：一段指令、一段查詢，或一個來源網址

做不到就把它改寫成定性敘述，或者刪掉。

```
✗ 「大約兩成的公告帶多張證」
✓ 「41 / 206 = 19.9% 的公告對到超過一張證（2026-08-05）；重跑見 docs/03 §(a)」
```

理由很直接：一個沒有出處的數字，傳到第三份文件時就會被當成經過稽核的事實。
把數字寫出去之前該做的三個檢查在 [docs/07 §3](docs/07-controls-and-pitfalls.md)。

**逐字引文的框裡只放來源文字。** 你自己數出來的品項數、你對條文的解讀、
「自某年未修正」這類判斷，一律放在框外並標明是你說的。

---

## 2. 每一頁的結尾控制段，必須同時有正控與負控

沒有結尾控制段的文件不予合併。只有正控的也不予合併。

**正控只能證明工具會叫；只有負控能抓到工具叫太多。** 一個把所有東西都判成 true
的比對器，正控會完美通過。

而且**負控要取自最容易被誤判為正的那一族**。一個本來就不可能匹配的負控，
什麼也證明不了。可以直接抄的三個例子：

| 主題 | 正控 | 好的負控 | 沒有用的負控 |
|---|---|---|---|
| `07` 判定 | 帶 `07` 的新分子證 | `25 監視中學名藥`（名稱裡也有「監視」） | 一張化粧品許可證 |
| moiety 首見 | `DASATINIB`（2008） | `DASATINIB MONOHYDRATE`（2026，同分子） | `ASPIRIN` |
| 標題解析 | 一般單證公告 | 短尾範圍、公司名黏字別、R 序號 | 一句完全沒有許可證號的話 |

---

## 3. 不得新增網路測試

`make test` 必須在斷網狀態下通過。fixtures 已經夠用。

活體檢查只有一個，`make check-live`，它是**手動**跑的，而且刻意不接進任何
自動化——它會打到政府主機。

同理：**不接受任何批次抓取工具**，包括對明細 API 的批次查詢與對公告清單的
全量抓取。示範一次請求的形狀是教學；提供批次工具是把流量指向政府主機。

---

## 送出之前

- [ ] `make test` 通過（離線）
- [ ] 新增或修改的每個數字，都帶了日期、n 與重跑方式
- [ ] 動到的每一頁，結尾控制段仍然同時有正控與負控
- [ ] 沒有新增網路測試、沒有新增批次工具
- [ ] 沒有把資料鏡像進 `fixtures/`（樣本大小以測試需要為準）
- [ ] 沒有出現主機名、內網 IP、連線字串、絕對家目錄路徑
- [ ] 如果你改動了某個欄位的意思，你 grep 過欄位名並修掉了**每一個讀它的地方**，
      包括說明文字（見 [docs/08 §2](docs/08-if-you-build-a-ledger.md)）

## 回報錯誤

如果你發現本 repo 的某個數字是錯的，最有用的回報形式是**你的重跑指令與它的輸出**。
本 repo 已經自己撤回過十幾個數字（逐筆理由在 [`measurements.json`](measurements.json)
的 `deleted` 段落），每一次都是靠重跑，不是靠重讀。
